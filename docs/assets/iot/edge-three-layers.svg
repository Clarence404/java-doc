<svg viewBox="0 0 680 368" xmlns="http://www.w3.org/2000/svg" font-family="system-ui,sans-serif" font-size="12">
  <style>
    :root {
      --fg:#1a1a2e; --muted:#6b7280; --bg:#f8f9fc; --card:#ffffff; --border:#e2e8f0;
      --p:#6366f1; --b:#0284c7; --g:#059669; --y:#d97706; --r:#dc2626;
    }
    :root[data-theme="dark"] {
      --fg:#e5e7eb; --muted:#9ca3af; --bg:#1a1a2e; --card:#252540; --border:#3a3a5c;
      --p:#818cf8; --b:#38bdf8; --g:#34d399; --y:#fbbf24; --r:#f87171;
    }
    @media (prefers-color-scheme: dark) {
      :root { --fg:#e5e7eb; --muted:#9ca3af; --bg:#1a1a2e; --card:#252540; --border:#3a3a5c;
              --p:#818cf8; --b:#38bdf8; --g:#34d399; --y:#fbbf24; --r:#f87171; }
    }
    .layer { rx:10; ry:10; fill:var(--card); stroke-width:2; }
    .tag { font-size:13px; font-weight:700; }
    .item { font-size:11px; fill:var(--muted); }
    .arr { stroke-width:2; marker-end:url(#a); marker-start:url(#b); fill:none; }
    .proto { font-size:10px; fill:var(--muted); }
  </style>
  <defs>
    <marker id="a" markerWidth="7" markerHeight="5" refX="6" refY="2.5" orient="auto">
      <polygon points="0 0,7 2.5,0 5" fill="var(--muted)"/>
    </marker>
    <marker id="b" markerWidth="7" markerHeight="5" refX="1" refY="2.5" orient="auto">
      <polygon points="7 0,0 2.5,7 5" fill="var(--muted)"/>
    </marker>
  </defs>

  <!-- 云端 -->
  <rect class="layer" x="10" y="10" width="660" height="80" stroke="var(--b)"/>
  <text class="tag" x="340" y="36" text-anchor="middle" fill="var(--b)">云端（Cloud）</text>
  <text class="item" x="170" y="64" text-anchor="middle">全局数据存储 / 模型训练 / 大盘分析</text>
  <text class="item" x="510" y="64" text-anchor="middle">设备管理平台 / OTA 升级 / 告警中心</text>

  <!-- 双向箭头 + 协议 -->
  <line class="arr" x1="340" y1="90" x2="340" y2="130" stroke="var(--muted)"/>
  <text class="proto" x="348" y="120">MQTT / HTTP / gRPC</text>

  <!-- 边缘层 -->
  <rect class="layer" x="10" y="136" width="660" height="96" stroke="var(--y)"/>
  <text class="tag" x="340" y="160" text-anchor="middle" fill="var(--y)">边缘层（Edge）</text>
  <text class="item" x="170" y="183" text-anchor="middle">协议转换（Modbus / OPC-UA → MQTT）</text>
  <text class="item" x="510" y="183" text-anchor="middle">本地规则引擎（实时告警、联动控制）</text>
  <text class="item" x="340" y="206" text-anchor="middle">数据过滤聚合 · 断网续传缓存</text>

  <!-- 双向箭头 + 协议 -->
  <line class="arr" x1="340" y1="232" x2="340" y2="272" stroke="var(--muted)"/>
  <text class="proto" x="348" y="261">Modbus · OPC-UA · Zigbee</text>

  <!-- 设备端 -->
  <rect class="layer" x="10" y="278" width="660" height="70" stroke="var(--g)"/>
  <text class="tag" x="340" y="300" text-anchor="middle" fill="var(--g)">设备端（Device）</text>
  <text class="item" x="170" y="326" text-anchor="middle">传感器 · PLC · 摄像头 · 控制器 · 智能电表</text>
  <text class="item" x="510" y="326" text-anchor="middle">边缘网关 · RTU · 工业相机 · 智能仪表</text>
</svg>
